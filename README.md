# Nombre 
Luis Angel Gomez 

# Lo que aprendí hoy
...

# Programación Orientada a Objetos (POO) en PHP
 Creación de Clases y Objetos: Diseñé estructuras independientes (`Persona2`, `Producto`) con atributos públicos y sus respectivos métodos.
 
 # Método Constructor:
  Implementé `__construct` para asignar los valores iniciales a las propiedades de los objetos automáticamente usando `new`.

 # Herencia en PHP:
  Utilicé la palabra reservada `extends` para crear la clase `estudiante` basada en la clase padre `Persona`, logrando reutilizar código de forma eficiente.
 
 # Polimorfismo:
  Aprendí a modificar el comportamiento del método `saludar()` en la clase heredada para que muestre un comportamiento personalizado en el navegador.

